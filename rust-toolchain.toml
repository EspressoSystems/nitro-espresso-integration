[toolchain]
channel = "1.85.0"
components = ["llvm-tools-preview", "rust-src", "clippy", "rustfmt"]
targets = ["wasm32-unknown-unknown", "wasm32-wasi"]  # Changed from wasip1 to wasi
profile = "minimal"